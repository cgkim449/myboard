<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cgkim.myboard.dao.UserDao">

    <select id="selectByUserId" resultType="userVo">
        select user_id
             , username
             , nickname
          from user
         where user_id = #{userId}
    </select>

    <select id="selectByUsername" resultType="userVo">
        select user_id
             , username
             , password
             , nickname
             , user_register_date
             , user_update_date
          from user
         where username = #{username}
    </select>

    <select id="selectCountByUsername" resultType="java.lang.Integer">
        select count(*)
          from user
         where username = #{username}
    </select>

    <select id="selectCountByNickname" resultType="java.lang.Integer">
        select count(*)
          from user
         where nickname = #{nickname}
    </select>

    <insert id="insert" parameterType="signUpRequest" useGeneratedKeys="true" keyProperty="userId">
        insert into user
            (username, password, nickname)
        values
            (#{username}, #{password}, #{nickname})
    </insert>
</mapper>