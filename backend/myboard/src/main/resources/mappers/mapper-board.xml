<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cgkim.myboard.dao.BoardDao">

    <select id="selectList" resultType="boardListResponse" parameterType="boardSearchRequest">
        select b.board_id
             , b.category_id
             , b.board_title
             , b.board_content
             , b.board_writer
             , b.board_view_count
             , b.board_password
             , b.board_has_attach
             , b.board_register_date
             , b.board_update_date
             , ct.category_name
          from board as b
          join category ct
            on b.category_id = ct.category_id
        <include refid="boardSearchCondition"/>
      order by b.board_register_date desc, b.board_id desc
         limit #{offset}, #{limit}
    </select>

    <select id="selectCount" resultType="java.lang.Integer" parameterType="boardSearchRequest">
        select count(*)
          from board as b
        <include refid="boardSearchCondition"/>
    </select>

    <sql id="boardSearchCondition">
        <where>
            <if test="fromDate != null">
                b.board_register_date <![CDATA[>=]]> #{fromDate}
            </if>
            <if test="toDate != null">
                and b.board_register_date <![CDATA[<=]]> #{toDate}
            </if>
            <if test = "categoryId != 0 and categoryId != null">
                and b.category_id = #{categoryId}
            </if>
            <if test = "keyword != null">
                and (
                     b.board_title like concat('%',#{keyword},'%')
                     or b.board_content like concat('%',#{keyword},'%')
                     or b.board_writer like concat('%',#{keyword},'%')
                    )
            </if>
        </where>
    </sql>
</mapper>